# API base URL (no trailing slash)
# - Web: use http://localhost:8080 (backend on same machine)
# - iOS simulator: localhost works
# - Android emulator / physical device: app auto-uses your dev machine's IP from Expo
#   (same host as the Metro bundler). Only set this if auto-detection fails.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
